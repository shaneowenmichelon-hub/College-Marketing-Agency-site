# Thaw Out Music Festival — image assets

Drop the extracted contents of `sponsorphotos.zip` here.

- Rename to sequential kebab-case: `thaw-out-01.jpg`, `thaw-out-02.jpg`, …
- Resize to a max of 1600px on the long edge; keep aspect ratios.
- The gallery on the Events page reads `thaw-out-01.jpg … thaw-out-07.jpg`
  (see `gallery.files` in `src/site.config.ts`). Add or rename entries there
  to match the files you drop in.
- Any file listed but not present hides itself gracefully; if none are present,
  the gallery shows a "Request the album" fallback instead.

## Insight screenshots (rendered as captioned evidence in the proof block)
- `insights/views.png`   → captioned "Social reach"
- `insights/comments.png` → captioned "Giveaway engagement"

Only use images from the supplied `sponsorphotos.zip`. Do not source Thaw Out
imagery from the web, and do not add third-party brand logos.
